{
  "name": "internship-test-task",
  "version": "1.0.0",
  "description": "WIP.",
  "type": "module",
  "scripts": {
    "backend:dev": "pnpm -F @internship-test-task/backend dev",
    "backend:build": "pnpm -F @internship-test-task/backend build",
    "backend:start": "pnpm -F @internship-test-task/backend start",
    "backend:test": "pnpm -F @internship-test-task/backend test",
    "backend:test:watch": "pnpm -F @internship-test-task/backend test:watch",
    "backend:test:cov": "pnpm -F @internship-test-task/backend test:cov",
    "backend:test:debug": "pnpm -F @internship-test-task/backend test:debug",
    "backend:format": "pnpm -F @internship-test-task/backend format",
    "backend:lint": "pnpm -F @internship-test-task/backend lint",
    "backend:prebuild": "pnpm -F @internship-test-task/backend prebuild",
    "frontend:dev": "pnpm -F @internship-test-task/frontend dev",
    "frontend:build": "pnpm -F @internship-test-task/frontend build",
    "frontend:start": "pnpm -F @internship-test-task/frontend start",
    "frontend:preview": "pnpm -F @internship-test-task/frontend preview",
    "frontend:test": "pnpm -F @internship-test-task/frontend test",
    "frontend:api:sync": "pnpm -F @internship-test-task/frontend api:sync",
    "frontend:format": "pnpm -F @internship-test-task/frontend format",
    "frontend:lint": "pnpm -F @internship-test-task/frontend lint",
    "format": "pnpm -r format",
    "lint": "pnpm -r lint",
    "build": "pnpm -r --workspace-concurrency=1 build",
    "prepare": "husky",
    "docker:up": "docker compose -f docker-compose.dev.yaml --env-file=.env.dev up -d",
    "docker:down": "docker compose -f docker-compose.dev.yaml --env-file=.env.dev down"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Radency/internship-test-task.git"
  },
  "homepage": "https://github.com/Radency/internship-test-task#readme",
  "devDependencies": {
    "eslint-plugin-storybook": "^0.8.0",
    "husky": "9.0.11",
    "prettier": "3.2.5"
  }
}
